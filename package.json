{
  "devDependencies": {
    "prettier": "2.5.1"
  },
  "scripts": {
    "build": "npm run build --workspaces --if-present"
  },
  "workspaces": [
    "packages/transition",
    "packages/core",
    "packages/bootstrap"
  ]
}
