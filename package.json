{
  "devDependencies": {
    "prettier": "2.7.1"
  },
  "scripts": {
    "build": "npm run build --workspaces --if-present"
  },
  "workspaces": [
    "packages/transition",
    "packages/core",
    "packages/bootstrap",
    "start",
    "www"
  ]
}
